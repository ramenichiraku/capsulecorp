package Message;

import Frame.IFrameSuscriptor;

public interface IFrameMaster {
	
	abstract public void setOnEvent (IFrameSuscriptor subscriptor);
	
	abstract public boolean sendCommand (int command,int parameter);
}