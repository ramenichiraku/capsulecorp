package Message;

import Frame.IFrameSuscriptor;

public interface IMessageSlave{
	abstract public void setOnEvent (IFrameSuscriptor subscriptor);
	
	abstract public boolean sendEvent (int event, int prameter);
}